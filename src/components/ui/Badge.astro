---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`inline-flex items-center rounded-full px-4 py-2 bg-white/10 backdrop-blur-md border border-[var(--color-border-glass)] text-white text-sm font-medium ${className}`}>
  <slot />
</div>
